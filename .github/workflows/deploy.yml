
# github workflow definition
name: Deploy Infra
on: [push]

# tfsec block simulation for validator
terraform {
  required_version = ">= 0.14.9"
}

provider "azurerm" {
  features {}
}

resource "azurerm_resource_group" "rg" {
  name     = "upt-arg-secure"
  location = "eastus"
}

