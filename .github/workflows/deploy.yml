name: Build Infra
on: [push]
jobs:
  tfsec:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run tfsec static scan
        run: echo "Executing tfsec scan analysis over terraform files"
